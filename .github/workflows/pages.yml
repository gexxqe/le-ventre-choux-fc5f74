name: Deploy web to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "web/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Prepare Pages artifact
        run: |
          rm -rf _site
          cp -R web _site
          python3 - <<'PY'
          from pathlib import Path

          index = Path('_site/index.html')
          html = index.read_text(encoding='utf-8')
          tag = '<script src="./backend.js?v=20260917-2"></script>'
          if 'backend.js' not in html:
              html = html.replace('</body>', f'{tag}\n</body>')
          index.write_text(html, encoding='utf-8')

          admin = Path('_site/admin.html')
          html = admin.read_text(encoding='utf-8')
          tag = '<script src="./admin-fix.js?v=20260917-2"></script>'
          if 'admin-fix.js' not in html:
              html = html.replace('</body>', f'{tag}\n</body>')
          admin.write_text(html, encoding='utf-8')
          PY

      - name: Upload web artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
